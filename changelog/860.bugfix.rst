Fixes a false domain violation when indexing a symbol with a nested implicit set that spans multiple positions. The nested set's positions are now expanded correctly so subsequent indices stay aligned with the declared domain.
